# Render Blueprint - Deploy with one click
services:
  - type: web
    name: monetizacion-jlab
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT
    envVars:
      - key: PYTHON_VERSION
        value: "3.11.7"
      # Server-side Anthropic key enables the grounded AI chat.
      # Set this as a secret in the Render dashboard (never commit it).
      # Without it, the chat falls back to the local keyword engine.
      - key: ANTHROPIC_API_KEY
        sync: false
      - key: ANTHROPIC_MODEL
        value: claude-haiku-4-5
      - key: AI_MAX_TOOL_TURNS
        value: "6"
